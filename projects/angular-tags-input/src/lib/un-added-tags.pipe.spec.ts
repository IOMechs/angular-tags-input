import { UnAddedTagsPipe } from './un-added-tags.pipe';

describe('UnAddedTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new UnAddedTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
