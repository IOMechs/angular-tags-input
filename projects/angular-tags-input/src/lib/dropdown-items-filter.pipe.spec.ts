import { DropdownItemsFilterPipe } from './dropdown-items-filter.pipe';

describe('DropdownItemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownItemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
